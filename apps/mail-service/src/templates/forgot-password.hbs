<!DOCTYPE html>
<html lang="en" xmlns="http://www.w3.org/1999/xhtml">

<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Reset Your Password</title>
    <style>
        body {
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #f4f4f4;
        }

        .email-container {
            max-width: 100%;
            margin: auto;
            background-color: #ffffff;
            border-radius: 10px;
            overflow: hidden;
        }

        .header {
            background-image: url('{{backgroundImageUrl}}');
            background-size: cover;
            padding: 40px;
            text-align: center;
        }

        .header img {
            max-width: 120px;
            border-radius: 20px;
            cursor: not-allowed;
        }

        .content {
            padding: 30px;
            color: #333333;
        }

        .content h1 {
            font-size: 24px;
            margin-bottom: 20px;
        }

        .btn {
            display: inline-block;
            padding: 14px 24px;
            margin: 20px 0;
            background-color: #4CAF50;
            color: #ffffff;
            text-decoration: none;
            border-radius: 5px;
            font-weight: bold;
        }

        .footer {
            background-color: #f4f4f4;
            padding: 20px;
            text-align: center;
            font-size: 13px;
            color: #888888;
        }

        .social-icons img {
            width: 24px;
            margin: 0 5px;
        }

        @media only screen and (max-width: 600px) {
            .content {
                padding: 20px;
            }

            .btn {
                padding: 12px 20px;
            }
        }
    </style>
</head>

<body>
    <div class="email-container">
        <div class="header">
            <img style="cursor: not-allowed;" src="{{logoUrl}}" alt="Company Logo">
        </div>

        <div class="content">
            <h1>Hello {{name}},</h1>
            <p>You recently requested to reset your password for your {{companyName}} account.</p>
            <p>Click the button below to reset it. This link is valid for a limited time.</p>
            <a href="{{resetLink}}" class="btn">Reset Password</a>
            <p>If you did not request a password reset, please ignore this email or contact support if you have
                questions.</p>
            <p>Thanks,<br>The {{companyName}} Team</p>
        </div>

        <div class="footer">
            <div class="social-icons">
                <a href="{{facebookUrl}}"><img src="{{facebookIcon}}" alt="Facebook"></a>
                <a href="{{githubUrl}}"><img src="{{githubIcon}}" alt="Github"></a>
                <a href="{{linkedinUrl}}"><img src="{{linkedinIcon}}" alt="LinkedIn"></a>
            </div>
            <p>© {{year}} {{companyName}}. All rights reserved.</p>
        </div>
    </div>
</body>

</html>